## v1.0.14

### 입차순번 저장본 경량 읽기
- 입차순번은 Excel을 실행하지 않고 OneDrive/SharePoint로 동기화된 `.xlsx`·`.xlsm` 저장본에서 필요한 범위만 읽습니다.
- 웹·데스크톱 Excel이 열려 있거나 닫혀 있어도 동작하며, 입차순번 읽기 자체는 원본을 수정하거나 저장하지 않습니다.
- 일반 파일은 메모리에서 처리하고 32MB를 넘는 파일만 시스템 임시 저장소를 사용하며, 동기화 도중 파일이 바뀌거나 일시적으로 불완전하면 다시 읽습니다.
- 화면에 저장본 수정시각을 표시해 마지막 저장·동기화 시점을 확인할 수 있습니다.
- 입차순번 탭은 10분마다 한 번 자동 갱신하며 기존 `새로고침` 버튼으로 언제든 수동 갱신할 수 있습니다.
- 수식과 VBA는 UnHelper가 실행하지 않으므로 Excel에서 계산·저장되고 OneDrive 동기화가 끝난 값을 기준으로 합니다.
- RAW 시트 반영은 기존 Excel COM 저장 방식을 유지합니다.

### 트럭 물리 팔렛트 표시
- 같은 CBN이 여러 SKU를 담는 경우 최초 SKU 행에만 물리 팔렛트 숫자를 표시하고 이후 행은 `공유 N`으로 표시합니다.
- 숫자로 시작하는 팔렛트 값만 합산하면 입차순번에서 사용하는 차량별 고유 CBN 물리 팔렛트와 일치합니다.
- SKU별 유닛 수와 팔렛트당 유닛 계산은 원래 컨테이너 정보를 그대로 사용합니다.

### 입차순번 정합성 안내
- 각층 목표치 상단에 앱 RAW의 고단·양곡 차량/팔렛트 원본 집계를 표시합니다.
- Excel 층 배정에는 있지만 앱 RAW에 없는 `RAW 미매칭` 차량 수와, 앱 RAW에는 있지만 층을 연결하지 못해 제외된 차량/물리 팔렛트를 화면에서 바로 확인할 수 있습니다.
- 트럭 저장 표의 팔렛트 수와 컨테이너 팔렛트 합계가 다른 손상·부분 데이터는 명시적으로 차단합니다.
- 다중 SKU Milkrun의 팔렛트당 유닛·팔렛트 무게가 `?`여도 저장된 고단·양곡 분류는 입차순번 집계에 정상 반영됩니다.

### 환경
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
