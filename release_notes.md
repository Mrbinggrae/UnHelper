## v1.0.0

### 입차순번 정식 대시보드
- 연결된 Excel의 `입차순번` 시트를 외부대기, 출차, 각층 목표치와 차량 상세 현황으로 한 화면에 표시합니다.
- Excel의 `MBN…`·`tbn00…` 예약번호를 RAW의 `M…`·`T…` 번호와 연결해 금일 차량의 팔렛트 및 경량·중량·고단·양곡·미분류 수량을 계산합니다.
- RAW에 없는 전일 차량은 `AP/AW` 팔렛트 값을 한 번만 사용하며, 서로 다른 두 값은 합산하지 않고 확인 필요로 표시합니다.
- `AO11/AP11`은 각층 목표치의 T/M 합계로 정확히 표시합니다.

### 안정적인 Excel 새로고침
- 입차순번 탭에 처음 들어갈 때 한 번 자동 갱신하고, 이후에는 `새로고침` 버튼으로 최신 Excel 값을 다시 읽습니다.
- Excel이 열려 있으면 저장 전 현재 값을 그대로 읽고, 닫혀 있으면 읽기 전용으로 열어 확인한 뒤 종료합니다.
- RAW 데이터가 없으면 전일·금일을 잘못 구분하지 않도록 먼저 RAW 데이터를 얻거나 표를 가져오라는 안내를 표시합니다.

### 기존 자동화 유지
- Milkrun·트럭 다운로드, RAW 표 복원·공유, WMS 무게 측정·이어하기와 수동 고단·양곡 저장을 계속 지원합니다.
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
