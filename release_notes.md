## v0.1.17

### 복원·가져온 표 이어서 진행
- 같은 기준일의 트럭 또는 Milkrun 표가 있으면 `데이터 얻기`에서 진행 방법을 먼저 확인합니다.
- 앱 재실행으로 자동 복원된 표와 다른 사용자의 공유 파일에서 가져온 표도 동일하게 인식합니다.
- `이어서 진행`은 현재 표를 유지하고 저장 무게가 없는 SKU만 WMS에서 확인합니다.
- `처음부터 다시`를 선택한 경우에만 Shipments 조회, 다운로드, Excel 반영, 상세 상품 수집과 WMS 측정을 전부 다시 실행합니다.
- `나중에`를 선택하면 현재 표를 그대로 유지합니다.

### 기존 기능 유지
- 상세 상품 조회·WMS 측정 진행률과 미완료 체크포인트를 계속 지원합니다.
- 값이 같은 중복 SKU는 확인창 없이 유지하고 실제 값이 다른 SKU만 덮어쓰기 여부를 확인합니다.

### 환경
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
