## v1.0.1

### 각층 목표 배치 상세
- `Raw_트럭`과 `Raw_밀크런`의 B열 층, C열 번호를 앱 RAW 표의 예약번호와 연결합니다.
- Excel 번호에는 접두사가 없으므로 원본은 그대로 두고, 비교할 때만 트럭은 `T`, Milkrun은 `M`을 붙입니다.
- 1F·2F별 T/M 차량 대수, 목표 대수, 총 팔렛트와 경량·중량·고단·양곡·미분류 팔렛트를 표시합니다.
- 외부대기 차량도 예정 층을 확인할 수 있고, 층 충돌이나 RAW 미매칭은 비고에 표시합니다.

### 입차순번 v1.0 기능 유지
- 탭 최초 자동 갱신과 수동 새로고침, 열린 Excel의 현재 값 읽기, RAW 없음 사전 안내를 유지합니다.
- `AO11/AP11` T/M 합계와 RAW에 없는 전일 차량의 `AP/AW` 단일 팔렛트 보완 규칙을 유지합니다.

### 환경
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
