## v1.0.13

### 저장된 WMS 무게 재사용
- `처음부터 다시`를 선택해도 Milkrun·트럭의 Shipments 다운로드와 상세 상품만 처음부터 다시 수집합니다.
- 상품 메모리에 WMS 무게가 저장된 SKU는 상품관리 페이지에서 재측정하지 않고 현재 유닛·팔렛트 수로 로컬 재계산합니다.
- 실제로 무게가 없는 SKU만 WMS 상품관리에서 조회합니다.
- 오래된 체크포인트가 저장 무게를 다시 미완료로 만들거나, 삭제된 무게를 완료로 표시하지 않도록 상품 메모리를 기준으로 정합성을 맞춥니다.
- 저장된 고단·양곡 수동 분류는 기존과 동일하게 유지됩니다.

### 검증 범위
- Milkrun과 트럭 각각에서 저장 무게 SKU와 미측정 SKU가 함께 있을 때 저장 무게는 캐시로 처리되고 미측정 SKU만 조회되는지 검증했습니다.
- `이어서 진행`과 `처음부터 다시` 모두 저장 무게를 강제로 새 측정하지 않는 회귀 테스트를 추가했습니다.

### 환경
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
