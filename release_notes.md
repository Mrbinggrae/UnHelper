## v1.0.12

### 입차순번 차량 분류
- 트럭·Milkrun 차량에 고단 SKU가 하나라도 있으면 차량 전체 팔렛트를 고단으로, 고단 없이 양곡 SKU가 있으면 차량 전체를 양곡으로 집계합니다.
- 수동 분류는 앱 재시작, 날짜 표 복원, 다른 사용자의 표 가져오기 이후에도 외부대기·출차·각층 목표치에 동일하게 반영됩니다.
- 상품 분류 가져오기에서 동일값은 묻지 않고, 실제 값이 다른 중복 SKU만 덮어쓰기 또는 기존 유지를 선택합니다.

### 차량 및 팔렛트 정확성
- 같은 거래처라도 배차번호가 다른 Milkrun 차량은 표 셀과 팔렛트 수를 별도로 유지합니다.
- 트럭 상세에서 같은 컨테이너 Barcode가 여러 SKU 행에 반복돼도 차량 총 팔렛트에는 한 번만 반영합니다.
- `RAW 미매칭` 도움말은 현재 앱 RAW 표에 같은 예약번호가 없어 상세 합계에서 제외된 차량 수와 일부 번호만 간결하게 보여줍니다.

### 예약 카드 및 상세 미확인 처리
- Milkrun과 트럭 모두 HTML의 `<b>` 강조 위치에 의존하지 않고 카드 전체 텍스트의 명시적인 `M…`/`T…` 번호로 정확히 일치시킵니다.
- `발송 확정` 등 상태 문구가 예약번호보다 먼저 표시돼도 트럭 예약을 찾으며, Milkrun의 여러 발주서 번호는 배차번호로 오인하지 않습니다.
- 상세를 아직 확인할 수 없는 트럭 예약도 다운로드 원본의 거래처·예약번호·유닛·팔렛트 합계를 미분류로 표시합니다. SKU 무게와 분류는 제외되며 `데이터 얻기`를 다시 누르면 Shipments 상세부터 재조회합니다.

> 이전 버전에서 저장한 트럭 표에는 컨테이너 Barcode가 없으므로 공유 컨테이너가 있는 날짜는 업데이트 후 `데이터 얻기`를 한 번 다시 실행해야 정확한 중복 제거 정보가 저장됩니다.

### 환경
- ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
