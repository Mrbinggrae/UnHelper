## v0.1.10

### 트럭 상세 및 무게 계산
- 트럭 일별 입고 상세의 실제 `truckContainerList` PALLET 컨테이너 표에서 SKU ID, SKU 명, 컨테이너 수량과 총 수량을 읽습니다.
- 같은 SKU가 여러 PALLET 행에 있으면 컨테이너 수량과 총 수량을 합산해 SKU별 팔렛트 수와 유닛 수로 표시합니다.
- 트럭도 각 SKU의 팔렛트당 유닛과 1팔렛트 무게를 계산해 경량·중량을 자동 분류하고 메모리에 저장합니다.

### 다중 SKU 차량 표시
- Milkrun과 트럭 모두 같은 차량의 거래처 이름과 밀크런 번호·예약번호 셀을 병합합니다.
- 같은 차량의 SKU 행에 교차 배경색을 적용해 그룹 경계를 쉽게 구분할 수 있습니다.
- 팔렛트 수, 박스·유닛 수, 무게와 분류는 SKU별로 독립 표시하고 저장합니다.

### 사용성
- 메인 화면 하단 콘솔 로그에 접기/펼치기 버튼을 추가하고 상태를 저장합니다.

### 환경
- 공용 ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
