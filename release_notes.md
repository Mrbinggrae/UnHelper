## v0.1.7

### RAW > 트럭
- 오늘 날짜의 트럭 입고예약 목록을 내려받아 `Raw_트럭!C1:U1000`에 값으로 반영합니다.
- C열 예약번호를 `T<숫자>` 형식으로 연결해 일별 입고의 T 카드 상품을 모두 수집한 뒤 WMS 조회를 시작합니다.
- M열 유닛 수와 N열 팔렛트 수로 팔렛트당 유닛과 1팔렛트 무게를 계산하며 280kg 기준으로 경량·중량을 표시합니다.
- 잘못된 예약번호, 수량 누락 또는 충돌은 기존 Excel 값을 지우기 전에 차단합니다.

### 분류 및 캐시
- 저장된 SKU 무게는 WMS 재접속 없이 현재 박스/유닛과 팔렛트 비율로 다시 계산합니다.
- 수동 경량·중량은 다음 조회에서 자동 계산으로 돌아가고 수동 고단만 유지합니다.
- 분류 버튼 열을 넓혀 경량·중량·고단 텍스트가 잘리지 않도록 개선했습니다.

### 환경
- 공용 ChromeDriver `151.0.7922.77` 및 Chrome 151 환경을 유지합니다.
